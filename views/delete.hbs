<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/delete.css">
    <title>Delete User</title>
</head>
<body>
    {{>Nav}}
    <h1>Delete User</h1>
    {{#if deletedName}}
    <p>User {{deletedName}} with id {{deletedUserId}} has been deleted</p>
    {{/if}}

    {{#if alreadyDeleted}}
         <p>User {{userName}} with id {{id}} has already been deleted.</p>
    {{/if}}

    {{#if existingBlogs}}

        <p>Cannot delete {{userName}} with id {{id}} without deleting the user's blogs first.</p>

        <a class="blogs-link"href="/allblogs/{{id}}">Go to Blogs</a>

    {{/if}}
</body>
</html>